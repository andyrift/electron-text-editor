# better_text_editor

1. Install dependencies
    ```sh
    npm install
    ```

2. Rebuild native modules for work electron
    ```sh
    npm run rebuild
    ```

3. Start the app
    ```sh
    npm run dev
    ```
